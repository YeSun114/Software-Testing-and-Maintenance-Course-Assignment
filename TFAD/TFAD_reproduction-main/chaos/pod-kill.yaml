apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: front-end-pod-kill
  namespace: sock-shop
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - sock-shop
    labelSelectors:
      name: front-end