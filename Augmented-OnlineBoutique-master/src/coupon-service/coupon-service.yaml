apiVersion: apps/v1
kind: Deployment
metadata:
  name: coupon-service
  namespace: hipster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coupon-service
  template:
    metadata:
      labels:
        app: coupon-service
    spec:
      containers:
      - name: server
        image: coupon-service:v1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080

---
apiVersion: v1
kind: Service
metadata:
  name: coupon-service
  namespace: hipster
spec:
  type: ClusterIP
  selector:
    app: coupon-service
  ports:
  - name: http
    port: 8080
    targetPort: 8080
